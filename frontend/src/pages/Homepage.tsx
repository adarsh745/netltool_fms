import Navbar, { NavItem, } from "../components/Home/Navbar";

const Homepage = () => {
  return (
    <div className="flex">
      <Navbar
        title="Netltool_FMS"
      />
    </div>
  );
};

export default Homepage;